# 巡件坐标（map 坐标系）

# 机器人出生点：每次启动固定在 Gazebo 的同一位置
INITIAL_POSE = (0.0420, 0.0090, 0.0)

# 三个巡件目标点：(x, y, yaw)，自选，含绕障碍路线
WAYPOINTS = [
    (3.5460, 1.8833, 0.0),   # 目标 1
    (2.6775, -1.2180, 0.0),  # 目标 2
    (1.4304, 1.1413, 0.0),   # 目标 3
]

# 单个目标失败后的重试次数（重试前清空代价地图）
RETRIES = 2
